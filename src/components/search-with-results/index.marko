class {

  onCreate(){
    this.state = { term:"eBay"};
  }

  /**
   * @param {string}
   */
  hasSearched(term){
    this.state.term = term;
  }
}

<search-bar on-search("hasSearched")/>
<search-results term=state.term/>
